interface:
  display_name: "Bootstrap Agent Team"
  short_description: "Guide me from my goal to a confirmed AI team"
  default_prompt: "Use $bootstrap-agent-team to inspect my project read-only, ask only high-impact questions, recommend a team in plain language, show me the exact plan, wait for confirmation, then create and validate it without enabling external integrations."
