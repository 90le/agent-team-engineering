interface:
  display_name: "Bootstrap Agent Team"
  short_description: "Build a native team for an AI host you already use"
  default_prompt: "Use $bootstrap-agent-team to inspect my project and installed AI hosts read-only, recommend a native expert team, show the exact plan, and wait for confirmation before creating anything."
