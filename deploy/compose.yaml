services:
  reference-simulation:
    build:
      context: ..
      dockerfile: deploy/Containerfile
    command: ["simulate", "--approve-production"]
    read_only: true
    network_mode: none
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=16m
