[project]
name = "agent-team-engineering"
version = "0.7.0"
description = "Context-first, vendor-neutral factory for portable AI teams and governed software delivery"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
