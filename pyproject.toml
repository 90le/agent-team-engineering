[project]
name = "agent-team-engineering"
version = "0.9.0"
description = "Host-native factory for portable AI teams, Skills, context, and governed software delivery"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
