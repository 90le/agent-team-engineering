# {{INSTANCE_NAME}}

Instance ID: `{{INSTANCE_ID}}`

This private repository is an Agent Team instance generated by `{{FACTORY_ID}}` version `{{FACTORY_VERSION}}`. It stores declarative configuration, policy, version locks, operating documentation, and reviewable evidence. It must not store secret values, runtime databases, model conversations, build workspaces, or application source history.

Start with [AI-BOOTSTRAP.md](AI-BOOTSTRAP.md). The selected team pack is `{{TEAM_PACK_ID}}` version `{{TEAM_PACK_VERSION}}`.

The Factory repository, this instance repository, and managed application repositories are separate authorities. Changing one does not silently upgrade the others.
