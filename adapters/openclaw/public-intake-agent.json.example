{
  "profile": "public-intake-reference",
  "adapter_slot": "intake",
  "workspace_access": "none",
  "sandbox_scope": "agent",
  "allowed_tools": ["feedback.submit"],
  "denied_tools": ["shell", "exec", "git", "browser", "deploy", "secrets", "agent-to-agent"],
  "output_contract": "schemas/feedback-event.schema.json",
  "note": "Map these reference controls to the installed OpenClaw version; this is not a drop-in production config."
}
